# MaaAuto 主程序依赖
# 注：pyinstaller 只用于打包，运行时不需要，但保留方便开发者一键安装

PySide6>=6.6.0
pyautogui>=0.9.54
psutil>=5.9.0
pywin32>=306
Pillow>=10.0.0
packaging>=23.0

# 打包工具（build.py / publish.py 用）
pyinstaller>=6.0.0